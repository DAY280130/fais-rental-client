import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fais-rental-client',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
