import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fais.faisrental',
  appName: 'Fais-Rental',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
